---
export interface Props {
  href: string;
  variant?: "affiliate" | "leadmagnet" | "primary" | "secondary";
  rel?: string;
  target?: string;
  ctaType?: string;
  class?: string;
}

const {
  href,
  variant = "primary",
  rel,
  target,
  ctaType,
  class: className = "",
} = Astro.props;

const baseClasses =
  "inline-flex items-center justify-center px-5 py-3 rounded-md font-semibold text-sm transition-colors no-underline";

const variantClasses = {
  affiliate: "bg-gold text-white hover:bg-gold-hover",
  leadmagnet: "bg-navy-deep text-white hover:bg-navy-charcoal",
  primary: "bg-gold text-white hover:bg-gold-hover",
  secondary: "bg-canvas-card text-navy-deep border border-line hover:bg-line",
}[variant];

const computedRel =
  rel ?? (variant === "affiliate" ? "sponsored noopener noreferrer" : undefined);
const computedTarget = target ?? (variant === "affiliate" ? "_blank" : undefined);
---

<a
  href={href}
  class={`${baseClasses} ${variantClasses} ${className}`}
  rel={computedRel}
  target={computedTarget}
  data-cta-type={ctaType ?? variant}
>
  <slot />
</a>
